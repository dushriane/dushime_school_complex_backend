# Example env values
# Use PostgreSQL when running with docker-compose (the compose file sets DATABASE_URL automatically)
DATABASE_URL=postgresql://postgres:postgres@db:5432/dushime
# Or for local SQLite (uncomment to use):
# DATABASE_URL=sqlite:///./dushime.db

SMTP_HOST=localhost
SMTP_PORT=25
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@dushime-school.rw
ADMIN_EMAIL=info@dushime-school.rw
